function setfulltext(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('full');
    textbox.value = it;
}
function setAvailableCoinsText(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('acoin');
    textbox.value = it;
}
function setAvailableSpacesText(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('aspace');
    textbox.value = it;
}
function setEntryBarrierText(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('entry');
    textbox.value = it;
}
function setExitBarrierTextp(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('exit');
    textbox.value = it;
}
function setCoinMachineText(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('coin');
    textbox.value = it;
}
function setPaymentDisplayText(text)// Pointer_stringify(text)
{
    var it = Pointer_stringify(text);
    var textbox = document.getElementById('payment');
    textbox.value = it;
}